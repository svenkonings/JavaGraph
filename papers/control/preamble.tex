\usepackage{graphicx}
\usepackage{subfigure}
\usepackage{listings}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{verbatim}
\usepackage{ifthen}
\usepackage{tikz}
\usetikzlibrary{arrows,automata,positioning}

% An environment for re-stating and proving results in an appendix.
% The proof will be labelled \prooflabel{#2}
% #1 the kind of result (theorem, lemma etc)
% #2 the label of the original result
\newenvironment{oldresult}[2]{%
    \label{\prooflabel{#2}}
    \csname @begintheorem \endcsname{\bf#1}{\bf { }\ref{#2}}%
}{%
    \csname @endtheorem \endcsname
}

% The label for the proof of an old result
\newcommand{\prooflabel}[1]{#1-proof}

\usepackage{color}
\definecolor{green}{rgb}{0,.47,0}
\definecolor{gray}{rgb}{.5,.5,.5}
\definecolor{lightgray}{rgb}{.75,.75,.75}
\definecolor{backgray}{gray}{.95}

% Define compose star shit:
\lstdefinelanguage{Control}
{	morekeywords={alap,while,do,if,try,else,choice,until},
	sensitive=true,
	morecomment=[l]{//},
	morecomment=[s]{/*}{*/}
}

% Setup stuff for Control src
\lstset{
language=Control,
	tabsize=3,
	basicstyle=\ttfamily,
	keywordstyle=\bf,
	commentstyle=\color{gray}\it,
	showstringspaces=false,
%	numbers=left,
%	numberstyle=\color{gray},
%	stepnumber=1,
%	numbersep=7pt,
	frame=ltrb,
	breaklines = true,
	captionpos=b%,
%	backgroundcolor=\color{backgray}
}

\newboolean{full}
\setboolean{full}{false}

\graphicspath{{./images/}}

\setlength{\floatsep}{\medskipamount}
\setlength{\textfloatsep}{\medskipamount}
\setlength{\intextsep}{\medskipamount}

\topmargin 0.65in 
\sloppy
