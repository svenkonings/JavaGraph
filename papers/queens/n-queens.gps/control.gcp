alap place; // Try to place queens as long as possible.
rename; // Rename the edges to create a symmetric board.
alap { // Mark the final states with the number of queens placed.
	try queen-10;
	else try queen-9;
	else try queen-8;
	else try queen-7;
	else try queen-6;
	else try queen-5;
	else try queen-4;
	else try queen-3;
	else try queen-2;
	else try queen-1;
}