package control;

recipe ClassLitAccess(node root, node exit, out node entry) {
	// A .class literal is always a leaf in the AST, so we close the recursion here.
	entry := rule.ClassLitAccess(root, exit);
}
