package control;

recipe UnaryExpr(node root, node exit, out node entry) {
	// Match the sub-component of the expression.
	node croot := rule.UnaryExpr(root, exit);
	// Build the sub-expression.
	entry := Expr(croot, exit);
}
