package control;

recipe SubRoutineStmt(node root, node exit, out node entry) {
	// Recipe for handling all subtypes of the abstract type 'SubRoutineStmt'.

	choice entry := SynchronizedStmt(root, exit);
	or entry := TryStmt(root, exit);
}
