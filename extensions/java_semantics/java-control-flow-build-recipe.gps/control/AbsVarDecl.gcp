package control;

recipe AbsVarDecl(node root, node exit, out node entry) {
	// Recipe for handling all subtypes of the abstract type 'AbsVarDecl'.
	
	choice entry := FieldDecl(root, exit);
	or entry := LocalDecl(root, exit);
	// Note: everything from this subtype hierarchy could possibly be
	// merged here.
}
